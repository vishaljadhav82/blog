/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com','placehold.co'], // Add your Firebase Storage domain here
      },
};

export default nextConfig;
